import { PAGE_TITLES } from '@/constants'
import type { ResumeContent } from './resume.types'

export const RESUME_CONTENT: ResumeContent = {
  heading: PAGE_TITLES.resume,
  intro: 'View my resume online or download a PDF copy to keep.',
  fileUrl: '/resume.pdf',
  fileName: 'Hasibe-Esra-Ergun-Resume.pdf',
}
